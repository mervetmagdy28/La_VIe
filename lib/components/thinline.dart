import 'package:flutter/material.dart';
class ThinLine extends StatelessWidget {
  const ThinLine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 2,
      color: Color(0xff8A8A8A),
    );
  }
}
