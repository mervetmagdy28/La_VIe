import 'package:flutter/material.dart';

class OnceScreen extends StatelessWidget {
  const OnceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
